- if notice
  .alert.warning
    = notice
.container
  .row
    .col.s12.m12.center
      .card-panel.teal.white-text
        %h2 Welcome! #{current_user.username}
        %span{class: 'white-text'}
          You are managing #{current_user.enterprise.name}